# 附錄

> **文件編號**: APPENDIX-00  
> **文件名稱**: 系統附錄  
> **版本**: v2.0  
> **最後更新**: 2025-12-31  
> **狀態**: Draft

---

## 📑 目錄

1. [參考資源](#1-參考資源)
2. [常見問題 (FAQ)](#2-常見問題-faq)
3. [專有名詞索引](#3-專有名詞索引)
4. [縮寫對照表](#4-縮寫對照表)
5. [完整配置檔範例](#5-完整配置檔範例)

---

## 1. 參考資源

### 1.1 官方文檔

**Spring 框架**:
- Spring Boot: https://spring.io/projects/spring-boot
- Spring Data JPA: https://spring.io/projects/spring-data-jpa
- Spring Security: https://spring.io/projects/spring-security
- Spring Boot 官方指南: https://docs.spring.io/spring-boot/docs/current/reference/html/

**資料庫**:
- PostgreSQL 15 文檔: https://www.postgresql.org/docs/15/
- Redis 文檔: https://redis.io/documentation
- MyBatis 官方文檔: https://mybatis.org/mybatis-3/

**開發工具**:
- Maven: https://maven.apache.org/guides/
- Docker: https://docs.docker.com/
- Kubernetes: https://kubernetes.io/docs/

**API 文檔工具**:
- Swagger UI: https://swagger.io/tools/swagger-ui/
- SpringDoc OpenAPI: https://springdoc.org/

### 1.2 資料來源

**股票資料**:
- Yahoo Finance: https://finance.yahoo.com/
- 台灣證券交易所: https://www.twse.com.tw/
- 櫃檯買賣中心: https://www.tpex.org.tw/
- 公開資訊觀測站: https://mops.twse.com.tw/
- FinMind API: https://finmindtrade.com/

**技術分析庫**:
- pandas-ta (Python): https://github.com/twopirllc/pandas-ta
- TA-Lib: https://ta-lib.org/

### 1.3 學習資源

**技術分析**:
- Investopedia 技術分析: https://www.investopedia.com/technical-analysis-4689657
- TradingView 技術分析教學: https://www.tradingview.com/wiki/

**程式開發**:
- Baeldung (Spring Boot): https://www.baeldung.com/
- JPA/Hibernate 教學: https://www.baeldung.com/learn-jpa-hibernate

---

## 2. 常見問題 (FAQ)

### 2.1 架構設計相關

**Q1: 為什麼使用 Signal Contract 統一信號格式？**

A: 確保不同模組產生的信號格式一致，方便去重、合併、評分，也便於未來擴充新的信號來源。統一格式的好處包括：
- 簡化信號處理邏輯
- 便於實作信號去重演算法
- 支援跨模組的信號比較與評分
- 提供一致的 API 回應格式

---

**Q2: 為什麼需要 Job 執行記錄？**

A: 追蹤每次批次任務的執行狀態，方便除錯與問題排查，也支援失敗重跑。具體好處：
- 可追溯性：了解每次 Job 的執行情況
- 除錯便利：失敗時可檢視錯誤訊息與統計資訊
- 監控指標：分析 Job 執行時間、成功率
- 冪等性支援：避免重複執行產生重複資料

---

**Q3: 為什麼使用 Redis？**

A: 加速熱資料查詢（如股價、指標），降低資料庫負載，提升系統效能。Redis 的應用場景：
- L1 快取：熱門股票的股價與指標
- Session 儲存：使用者登入狀態
- 分散式鎖：防止 Job 重複執行
- 排行榜：股票漲跌幅排行（使用 Sorted Set）

---

**Q4: 如何確保資料一致性？**

A: 多層保障機制：
1. **資料庫層級**：使用 UNIQUE 約束、外鍵約束
2. **應用層級**：使用資料庫交易 (@Transactional)
3. **資料品質檢核**：遵守總綱 4.7 資料品質規範
4. **冪等性設計**：使用 ON CONFLICT DO UPDATE

---

**Q5: 為什麼不使用微服務架構？**

A: 初期系統規模不大，單體應用更簡單、易維護。考量因素：
- **簡單性**：單體應用部署、測試更簡單
- **效能**：避免跨服務網路呼叫開銷
- **團隊規模**：小團隊更適合單體架構
- **未來擴展**：設計時保持模組獨立，未來可重構為微服務

---

### 2.2 技術實作相關

**Q6: 為什麼混合使用 JPA 和 MyBatis？**

A: 發揮兩者優勢，提升開發效率：
- **JPA**：適合簡單 CRUD、標準查詢、物件導向操作
- **MyBatis**：適合複雜查詢、動態 SQL、批次操作
- **混合使用**：根據場景選擇最適合的工具

範例：
```java
// JPA: 簡單查詢
stockRepository.findByStockId("2330");

// MyBatis: 複雜查詢
stockMapper.findStocksWithTechnicalAnalysis(criteria);
```

---

**Q7: 如何處理外部 API 限制？**

A: 多重策略：
1. **多資料源備援**：Yahoo Finance → 證交所 API → FinMind API
2. **快取減少請求**：熱門股票快取 1 小時
3. **請求頻率控制**：使用 Rate Limiter
4. **付費 API 備援**：關鍵時刻使用付費 API

---

**Q8: 測試環境如何模擬外部 API？**

A: 使用 Mock 工具：
- **WireMock**：模擬 HTTP API 回應
- **Testcontainers**：啟動真實的 PostgreSQL、Redis 容器
- **@MockBean**：Mock Service 層依賴

範例：
```java
@Test
public void testFetchStockPrice() {
    // WireMock 模擬 Yahoo Finance API
    stubFor(get(urlEqualTo("/quote/2330.TW"))
        .willReturn(aResponse()
            .withStatus(200)
            .withBody("{\"price\": 580.00}")));
    
    // 執行測試
    StockPrice price = stockService.fetchPrice("2330");
    assertEquals(580.00, price.getClosePrice());
}
```

---

### 2.3 效能與優化相關

**Q9: 如何處理大量歷史資料查詢？**

A: 多種優化策略：
1. **分區表**：stock_prices 按年份分區
2. **索引優化**：在常用查詢欄位建立索引
3. **批次查詢**：避免 N+1 查詢問題
4. **分頁查詢**：使用 LIMIT OFFSET
5. **資料歸檔**：舊資料歸檔到冷儲存

---

**Q10: Redis 快取何時失效？**

A: 三種失效策略：
1. **TTL 自動過期**：設定合理的 TTL（如 1 小時）
2. **主動更新**：資料更新時主動刷新快取
3. **LRU 淘汰**：記憶體不足時自動淘汰最少使用的資料

---

## 3. 專有名詞索引

| 術語 | 定義 | 參考章節 |
|-----|------|---------|
| **Signal Contract** | 統一信號資料模型 | 全系統契約 4.2 節 |
| **Job** | 可追蹤的批次作業單位 | 全系統契約 4.5 節 |
| **RBAC** | 基於角色的存取控制 | 全系統契約 4.6 節 |
| **Data Quality** | 資料品質規範 | 全系統契約 4.7 節 |
| **Idempotency** | 冪等性 | 全系統契約 4.5.6 節 |
| **NFR** | 非功能性需求 | NFR 文件第 7 章 |
| **DTO** | Data Transfer Object | 技術架構 5.2.6 節 |
| **Repository** | 資料存取層 | 技術架構 5.2.4 節 |
| **Entity** | JPA 實體類別 | 技術架構 5.2.3 節 |
| **Mapper** | MyBatis Mapper 接口 | 技術架構 5.2.5 節 |

---

## 4. 縮寫對照表

### 4.1 系統分析與設計

| 縮寫 | 全名 | 中文 |
|-----|------|------|
| SA | System Analysis | 系統分析 |
| SD | System Design | 系統設計 |
| NFR | Non-Functional Requirements | 非功能性需求 |
| FR | Functional Requirements | 功能性需求 |
| UC | Use Case | 使用案例 |
| ER | Entity-Relationship | 實體關聯 |
| ERD | Entity-Relationship Diagram | 實體關聯圖 |

### 4.2 技術架構

| 縮寫 | 全名 | 中文 |
|-----|------|------|
| API | Application Programming Interface | 應用程式介面 |
| REST | Representational State Transfer | 表現層狀態轉換 |
| JWT | JSON Web Token | JSON 網路令牌 |
| RBAC | Role-Based Access Control | 基於角色的存取控制 |
| CRUD | Create, Read, Update, Delete | 增查改刪 |
| ORM | Object-Relational Mapping | 物件關聯映射 |
| JPA | Java Persistence API | Java 持久化 API |
| DTO | Data Transfer Object | 資料傳輸物件 |
| DAO | Data Access Object | 資料存取物件 |
| MVC | Model-View-Controller | 模型-視圖-控制器 |

### 4.3 資料庫

| 縮寫 | 全名 | 中文 |
|-----|------|------|
| ACID | Atomicity, Consistency, Isolation, Durability | 原子性、一致性、隔離性、持久性 |
| PK | Primary Key | 主鍵 |
| FK | Foreign Key | 外鍵 |
| UK | Unique Key | 唯一鍵 |
| IDX | Index | 索引 |
| TTL | Time To Live | 存活時間 |

### 4.4 運維與監控

| 縮寫 | 全名 | 中文 |
|-----|------|------|
| RTO | Recovery Time Objective | 恢復時間目標 |
| RPO | Recovery Point Objective | 恢復點目標 |
| SLA | Service Level Agreement | 服務水平協議 |
| APM | Application Performance Monitoring | 應用效能監控 |
| K8s | Kubernetes | 容器編排平台 |

---

## 5. 完整配置檔範例

### 5.1 application.yml（完整版）

```yaml
server:
  port: 8080
  servlet:
    context-path: /
  shutdown: graceful

spring:
  application:
    name: stock-monitor-system

  # PostgreSQL 資料源配置
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:stock_monitor}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:password}
    driver-class-name: org.postgresql.Driver
    
    # HikariCP 連線池配置
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      pool-name: StockMonitorHikariPool
      leak-detection-threshold: 60000

  # JPA 配置
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate  # 生產環境使用 validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          batch_size: 20
          fetch_size: 50
        order_inserts: true
        order_updates: true
        generate_statistics: false

  # Redis 配置
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}
    database: 0
    timeout: 5000ms
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5
        max-wait: -1ms

  # Security 配置
  security:
    user:
      name: admin
      password: ${ADMIN_PASSWORD:admin123}

# MyBatis 配置
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.stockmonitor.entity
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: true
    lazy-loading-enabled: false
    aggressive-lazy-loading: false
    default-executor-type: REUSE
    default-statement-timeout: 25000
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  type-handlers-package: com.stockmonitor.typehandler

# 日誌配置
logging:
  level:
    root: INFO
    com.stockmonitor: DEBUG
    com.stockmonitor.mapper: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.jdbc.core: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file:
    name: logs/stock-monitor.log
    max-size: 10MB
    max-history: 30

# Actuator 監控配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: true

# 應用自定義配置
app:
  scheduler:
    enabled: true
    thread-pool-size: 10
  cache:
    default-ttl: 3600
  external-api:
    timeout: 10000
    retry-count: 3
```

### 5.2 pom.xml（完整版）

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.0</version>
        <relativePath/>
    </parent>

    <groupId>com.stockmonitor</groupId>
    <artifactId>stock-monitor-system</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <name>Stock Monitor System</name>
    <description>股票進出場監控系統</description>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <postgresql.version>42.7.1</postgresql.version>
        <mybatis-spring-boot.version>3.0.3</mybatis-spring-boot.version>
        <hypersistence-utils.version>3.7.0</hypersistence-utils.version>
        <springdoc-openapi.version>2.3.0</springdoc-openapi.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Data JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- MyBatis Spring Boot Starter -->
        <dependency>
            <groupId>org.mybatis.spring.boot</groupId>
            <artifactId>mybatis-spring-boot-starter</artifactId>
            <version>${mybatis-spring-boot.version}</version>
        </dependency>

        <!-- PostgreSQL Driver -->
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
        </dependency>

        <!-- Hibernate Types for PostgreSQL JSONB -->
        <dependency>
            <groupId>io.hypersistence</groupId>
            <artifactId>hypersistence-utils-hibernate-63</artifactId>
            <version>${hypersistence-utils.version}</version>
        </dependency>

        <!-- Redis -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- Spring Security -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- Spring Actuator -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- SpringDoc OpenAPI (Swagger) -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc-openapi.version}</version>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Testing -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
```

### 5.3 Docker Compose（開發環境）

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: stock-monitor-postgres
    environment:
      POSTGRES_DB: stock_monitor
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      PGDATA: /var/lib/postgresql/data/pgdata
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - stock-monitor-network

  redis:
    image: redis:7-alpine
    container_name: stock-monitor-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - stock-monitor-network
    command: redis-server --appendonly yes

volumes:
  postgres_data:
  redis_data:

networks:
  stock-monitor-network:
    driver: bridge
```

---

## 📚 相關文檔

- [系統功能總覽](./00-系統功能總覽.md)
- [技術架構](../technical/00-技術架構.md)
- [開發準則](../technical/00-開發準則.md)
- [名詞定義](../../user-guide/名詞定義.md)

---

**文件維護者**: 技術文件團隊  
**最後更新**: 2025-12-31  
**下次審核**: 2026-02-28
