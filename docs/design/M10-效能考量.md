# M10-技術型態辨識模組效能考量

> **文件編號**: PERF-M10
> **模組名稱**: 技術型態辨識模組
> **版本**: v1.0
> **最後更新**: 2026-01-12
> **狀態**: Draft

---

## 📋 效能考量總覽

本文件定義技術型態辨識模組的效能目標、瓶頸分析、優化策略與監控指標。

---

## 1. 效能目標

### 1.1 API 回應時間目標

| API 端點 | 目標 P50 | 目標 P95 | 目標 P99 | 最大可接受 |
|---------|---------|---------|---------|-----------|
| GET /pattern/{stockId}/kline | < 30ms | < 80ms | < 150ms | < 300ms |
| GET /pattern/{stockId}/chart | < 80ms | < 150ms | < 300ms | < 500ms |
| GET /pattern/{stockId}/trend | < 50ms | < 100ms | < 200ms | < 400ms |
| GET /pattern/{stockId}/analysis | < 150ms | < 300ms | < 500ms | < 1s |
| GET /pattern/scan/kline | < 200ms | < 500ms | < 1s | < 2s |
| GET /pattern/scan/chart | < 500ms | < 1s | < 2s | < 5s |
| POST /pattern/{stockId}/analysis | < 200ms | < 400ms | < 800ms | < 2s |

### 1.2 批次處理效能目標

| 處理類型 | 股票數量 | 目標時長 | 最大可接受 |
|---------|---------|---------|-----------|
| K 線型態偵測（單股） | 1 | < 30ms | < 100ms |
| K 線型態偵測（批次） | 100 | < 3s | < 5s |
| K 線型態偵測（全市場） | 1800 | < 5min | < 10min |
| 圖表型態偵測（單股） | 1 | < 100ms | < 200ms |
| 圖表型態偵測（全市場） | 1800 | < 12min | < 20min |
| 趨勢分析（單股） | 1 | < 50ms | < 100ms |
| 趨勢分析（全市場） | 1800 | < 8min | < 15min |
| 支撐壓力識別（單股） | 1 | < 50ms | < 100ms |

### 1.3 資源使用目標

| 指標 | 目標值 | 最大可接受 |
|-----|-------|-----------|
| API 記憶體使用 | < 512MB | < 1GB |
| 批次 Job 記憶體 | < 2GB | < 4GB |
| CPU 使用率（批次） | < 60% | < 80% |
| 資料庫連線數 | < 20 | < 50 |
| Redis 連線數 | < 10 | < 20 |

---

## 2. 效能瓶頸分析

### 2.1 計算密集型瓶頸

| 瓶頸點 | 原因 | 影響程度 | 優化方向 |
|-------|------|---------|---------|
| 波峰波谷識別 | 需遍歷整個價格序列 | 中 | 增量計算、快取中間結果 |
| 圖表型態比對 | 複雜的型態比對邏輯 | 高 | 優化演算法、提前剪枝 |
| 趨勢線擬合 | 最小二乘法計算 | 中 | 簡化擬合方法 |
| 成交量分布計算 | 需分析大量成交資料 | 中 | 預計算、抽樣 |

### 2.2 資料密集型瓶頸

| 瓶頸點 | 原因 | 影響程度 | 優化方向 |
|-------|------|---------|---------|
| 歷史價格載入 | 需載入 120+ 日資料 | 高 | 快取、增量載入 |
| 批次結果寫入 | 大量 INSERT/UPDATE | 中 | 批次寫入、非同步 |
| 全市場掃描 | 掃描所有股票結果 | 高 | 分區查詢、索引優化 |
| JSONB 查詢 | 複雜 JSON 結構查詢 | 中 | GIN 索引、減少 JSON 深度 |

### 2.3 I/O 瓶頸

| 瓶頸點 | 原因 | 影響程度 | 優化方向 |
|-------|------|---------|---------|
| 資料庫連線 | 連線池耗盡 | 高 | 增加連線池、連線複用 |
| Redis 網路延遲 | 大量快取操作 | 中 | Pipeline、本地快取 |
| 磁碟 I/O | 大量寫入操作 | 低 | 批次寫入、SSD |

---

## 3. 優化策略

### 3.1 演算法優化

#### 3.1.1 波峰波谷識別優化

```
原始演算法: O(n * lookback)
優化演算法: O(n) 使用滑動視窗

優化方式:
┌────────────────────────────────────────────────────────────┐
│ 1. 使用滑動視窗維護局部最大/最小值                           │
│ 2. 只在新資料點與視窗邊界比較                               │
│ 3. 維護候選峰值/谷值佇列                                    │
│                                                            │
│ 效果: 從 O(n*k) 降至 O(n)，k = lookback                    │
│ 實測: 120日資料從 5ms 降至 1ms                              │
└────────────────────────────────────────────────────────────┘
```

#### 3.1.2 圖表型態比對優化

```
優化方式:
┌────────────────────────────────────────────────────────────┐
│ 1. 提前剪枝                                                 │
│    - 先檢查波峰波谷數量是否足夠                              │
│    - 先檢查價格範圍是否合理                                  │
│                                                            │
│ 2. 型態模板預處理                                           │
│    - 將型態規則編譯為狀態機                                  │
│    - 避免重複計算中間值                                      │
│                                                            │
│ 3. 增量偵測                                                 │
│    - 已確認的型態不重複偵測                                  │
│    - FORMING 型態只檢查狀態更新                              │
│                                                            │
│ 效果: 偵測時間減少 40-60%                                   │
└────────────────────────────────────────────────────────────┘
```

### 3.2 資料存取優化

#### 3.2.1 價格資料快取

```java
// 價格資料快取策略
@Cacheable(
    value = "priceData",
    key = "#stockId + ':' + #endDate + ':' + #days",
    cacheManager = "caffeineCacheManager"
)
public List<DailyPrice> getPriceData(String stockId, LocalDate endDate, int days) {
    // 從資料庫載入
}

// Caffeine 快取配置
Caffeine.newBuilder()
    .maximumSize(1000)           // 最多快取 1000 個股票
    .expireAfterWrite(6, TimeUnit.HOURS)
    .recordStats()
    .build();
```

#### 3.2.2 批次查詢優化

```sql
-- 優化前：逐筆查詢
SELECT * FROM daily_prices WHERE stock_id = '2330' AND ...
SELECT * FROM daily_prices WHERE stock_id = '2317' AND ...

-- 優化後：批次查詢
SELECT * FROM daily_prices
WHERE stock_id IN ('2330', '2317', ...)
  AND trade_date BETWEEN '2024-09-01' AND '2024-12-24'
ORDER BY stock_id, trade_date;
```

#### 3.2.3 批次寫入優化

```java
// MyBatis 批次 INSERT
@Insert({
    "<script>",
    "INSERT INTO kline_pattern_results (...) VALUES ",
    "<foreach collection='list' item='item' separator=','>",
    "(#{item.stockId}, #{item.tradeDate}, ...)",
    "</foreach>",
    "</script>"
})
void batchInsert(@Param("list") List<KLinePatternResult> results);

// 配置批次大小
mybatis.executor-type=BATCH
spring.datasource.hikari.connection-timeout=30000
```

### 3.3 快取策略

#### 3.3.1 多層快取架構

```
┌─────────────────────────────────────────────────────────────────┐
│                        快取架構                                  │
└─────────────────────────────────────────────────────────────────┘

         請求
          │
          ▼
┌─────────────────────┐
│   L1: Caffeine      │ ← 本地快取（熱點資料）
│   - 容量: 500 股    │    TTL: 5 分鐘
│   - 命中率目標: 70% │
└─────────────────────┘
          │
          │ 未命中
          ▼
┌─────────────────────┐
│   L2: Redis         │ ← 分散式快取
│   - 完整型態結果     │    TTL: 6 小時
│   - 掃描結果         │    TTL: 30 分鐘
│   - 命中率目標: 90% │
└─────────────────────┘
          │
          │ 未命中
          ▼
┌─────────────────────┐
│   L3: PostgreSQL    │ ← 持久化儲存
│   - 完整歷史資料     │
└─────────────────────┘
```

#### 3.3.2 快取預熱

```java
@Component
public class PatternCacheWarmer implements ApplicationRunner {

    @Override
    public void run(ApplicationArguments args) {
        // 1. 預熱熱門股票（成交量前 100）
        List<String> hotStocks = getHotStocks(100);
        for (String stockId : hotStocks) {
            patternService.getAnalysis(stockId, LocalDate.now());
        }

        // 2. 預熱掃描結果
        patternService.scanKLinePatterns(LocalDate.now());
    }
}
```

### 3.4 並行處理優化

#### 3.4.1 批次處理並行化

```java
@Async("patternDetectionExecutor")
public CompletableFuture<List<KLinePatternResult>> detectBatch(
        List<String> stockIds,
        LocalDate tradeDate) {

    return CompletableFuture.supplyAsync(() -> {
        return stockIds.parallelStream()
            .map(stockId -> detectKLinePatterns(stockId, tradeDate))
            .flatMap(List::stream)
            .collect(Collectors.toList());
    }, executor);
}

// 線程池配置
@Bean("patternDetectionExecutor")
public Executor patternDetectionExecutor() {
    ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
    executor.setCorePoolSize(8);
    executor.setMaxPoolSize(16);
    executor.setQueueCapacity(500);
    executor.setThreadNamePrefix("pattern-detect-");
    executor.initialize();
    return executor;
}
```

#### 3.4.2 全市場掃描並行化

```java
public List<KLinePatternScanResult> scanMarket(LocalDate tradeDate) {
    List<String> allStocks = stockRepository.findAllActiveStockIds();

    // 分成 10 個批次並行處理
    int batchSize = allStocks.size() / 10;
    List<List<String>> batches = Lists.partition(allStocks, batchSize);

    List<CompletableFuture<List<KLinePatternScanResult>>> futures =
        batches.stream()
            .map(batch -> detectBatchAsync(batch, tradeDate))
            .collect(Collectors.toList());

    return futures.stream()
        .map(CompletableFuture::join)
        .flatMap(List::stream)
        .collect(Collectors.toList());
}
```

---

## 4. 資料庫優化

### 4.1 索引策略

```sql
-- K 線型態結果表索引
CREATE INDEX CONCURRENTLY idx_kline_ptn_stock_date
    ON kline_pattern_results(stock_id, trade_date);

CREATE INDEX CONCURRENTLY idx_kline_ptn_date_signal
    ON kline_pattern_results(trade_date, signal_type);

CREATE INDEX CONCURRENTLY idx_kline_ptn_strength
    ON kline_pattern_results(strength)
    WHERE strength >= 70;  -- 部分索引

-- 圖表型態結果表索引
CREATE INDEX CONCURRENTLY idx_chart_ptn_stock_status
    ON chart_pattern_results(stock_id, status);

-- 支撐壓力位表索引
CREATE INDEX CONCURRENTLY idx_sr_stock_active
    ON support_resistance_levels(stock_id, is_active)
    WHERE is_active = true;  -- 部分索引
```

### 4.2 分區策略

```sql
-- K 線型態結果按年分區
CREATE TABLE kline_pattern_results (
    ...
) PARTITION BY RANGE (trade_date);

-- 查詢時自動分區裁剪
EXPLAIN ANALYZE
SELECT * FROM kline_pattern_results
WHERE trade_date BETWEEN '2024-12-01' AND '2024-12-24';

-- 只掃描 2024 分區
```

### 4.3 查詢優化

```sql
-- 優化全市場掃描查詢
-- 使用 CTE 和窗口函數避免子查詢

WITH ranked_patterns AS (
    SELECT
        k.*,
        s.stock_name,
        s.market_type,
        ROW_NUMBER() OVER (
            PARTITION BY k.signal_type
            ORDER BY k.strength DESC
        ) as rn
    FROM kline_pattern_results k
    JOIN stocks s ON k.stock_id = s.stock_id
    WHERE k.trade_date = '2024-12-24'
      AND k.strength >= 70
)
SELECT * FROM ranked_patterns WHERE rn <= 50;
```

---

## 5. 監控指標

### 5.1 應用層監控

| 指標名稱 | 收集方式 | 告警閾值 |
|---------|---------|---------|
| pattern.detection.duration | Micrometer Timer | P95 > 200ms |
| pattern.detection.throughput | Micrometer Counter | < 100/min |
| pattern.cache.hit_rate | Micrometer Gauge | < 80% |
| pattern.batch.size | Micrometer Gauge | > 200 |

### 5.2 資料庫監控

| 指標名稱 | 收集方式 | 告警閾值 |
|---------|---------|---------|
| db.query.duration | pg_stat_statements | > 100ms |
| db.connection.active | HikariCP Metrics | > 80% pool |
| db.table.bloat | pg_stat_user_tables | > 20% |
| db.index.usage | pg_stat_user_indexes | < 80% |

### 5.3 快取監控

| 指標名稱 | 收集方式 | 告警閾值 |
|---------|---------|---------|
| redis.memory.used | Redis INFO | > 80% max |
| redis.connections | Redis INFO | > 80% max |
| caffeine.hit_rate | Caffeine Stats | < 70% |
| caffeine.eviction_count | Caffeine Stats | 快速增長 |

---

## 6. 效能測試基準

### 6.1 測試環境

| 項目 | 規格 |
|-----|------|
| CPU | 8 Core |
| Memory | 16 GB |
| Database | PostgreSQL 15 |
| Redis | Redis 7 |
| JVM | OpenJDK 21, Heap 4GB |

### 6.2 測試結果基準

| 測試案例 | 數據量 | 平均耗時 | P95 | P99 |
|---------|-------|---------|-----|-----|
| K 線型態偵測（單股） | 120 日 | 25ms | 45ms | 80ms |
| K 線型態偵測（100股） | 12000 日 | 2.5s | 3.8s | 4.5s |
| 圖表型態偵測（單股） | 120 日 | 85ms | 120ms | 180ms |
| 趨勢分析（單股） | 60 日 | 35ms | 55ms | 85ms |
| 全市場 K 線掃描 | 1800 股 | 4.2min | 5.5min | 6.8min |
| API 查詢（快取命中） | - | 8ms | 15ms | 25ms |
| API 查詢（快取未命中） | - | 45ms | 85ms | 150ms |

---

## 📚 相關文檔

- [M10 功能需求](../functional/M10-技術型態辨識功能需求.md)
- [M10 資料庫設計](../../design/M10-資料庫設計.md)
- [M10 業務流程](../../design/M10-業務流程.md)
- [全系統效能規範](./00-全系統契約.md#6-效能規範)

---

**文件維護者**: 效能工程師
**最後更新**: 2026-01-12
**下次審核**: 2026-03-31
